# Ainus 앱 코드 학습 가이드

> 초보자를 위한 상세 코드 설명서

이 문서는 Ainus AI 이슈 트래킹 앱의 주요 기능들이 어떻게 구현되었는지 초보자도 이해할 수 있도록 자세히 설명합니다.

---

## 목차

1. [프로젝트 개요](#1-프로젝트-개요)
2. [기본 구조 이해하기](#2-기본-구조-이해하기)
3. [홈 화면 (HomeScreen)](#3-홈-화면-homescreen)
4. [인사이트 화면 (InsightFeedScreen)](#4-인사이트-화면-insightfeedscreen)
5. [AI 추천 화면 (AIRecommendScreen)](#5-ai-추천-화면-airecommendscreen)
6. [이슈지수 화면 (IssueIndexScreen)](#6-이슈지수-화면-issueindexscreen)
7. [커뮤니티 화면 (CommunityScreen)](#7-커뮤니티-화면-communityscreen)
8. [공통 컴포넌트](#8-공통-컴포넌트)
9. [스타일링 시스템](#9-스타일링-시스템)
10. [초보자를 위한 팁](#10-초보자를-위한-팁)

---

## 1. 프로젝트 개요

### 1.1 Ainus 앱이란?

Ainus는 AI 관련 이슈를 추적하고 사용자에게 맞춤형 정보를 제공하는 React Native 모바일 앱입니다.

**주요 기능:**
- 실시간 AI 이슈 모니터링
- 직업별 맞춤 이슈 지수 제공
- AI 도구 추천
- 커뮤니티 게시판
- 인사이트 피드

### 1.2 사용된 기술 스택

```
React Native - 모바일 앱 프레임워크
JavaScript - 프로그래밍 언어
React Hooks - 상태 관리 (useState, useEffect)
StyleSheet - 스타일링
```

---

## 2. 기본 구조 이해하기

### 2.1 프로젝트 폴더 구조

```
Ainus_app/
├── src/
│   ├── screens/          # 화면 컴포넌트들
│   │   ├── HomeScreen.js
│   │   ├── InsightFeedScreen.js
│   │   ├── AIRecommendScreen.js
│   │   ├── IssueIndexScreen.js
│   │   └── CommunityScreen.js
│   ├── components/       # 재사용 가능한 컴포넌트들
│   │   └── common/
│   │       ├── Button.js
│   │       ├── Card.js
│   │       └── InsightCard.js
│   └── theme/           # 디자인 시스템
│       ├── colors.js    # 색상 정의
│       ├── spacing.js   # 간격 정의
│       └── typography.js # 글꼴 정의
```

### 2.2 React Native 기본 개념

#### 2.2.1 컴포넌트란?

컴포넌트는 UI의 작은 조각입니다. 레고 블록처럼 조립해서 화면을 만듭니다.

```javascript
// 간단한 컴포넌트 예시
const MyComponent = () => {
  return (
    <View>
      <Text>안녕하세요!</Text>
    </View>
  );
};
```

#### 2.2.2 React Native 기본 요소들

```javascript
<View>      // HTML의 <div>와 비슷 - 컨테이너
<Text>      // 텍스트 표시
<TouchableOpacity>  // 터치 가능한 버튼
<ScrollView>  // 스크롤 가능한 영역
<TextInput>   // 입력 필드
```

#### 2.2.3 Props (속성)

컴포넌트에 데이터를 전달하는 방법입니다.

```javascript
// 부모 컴포넌트
<Button title="클릭하세요" onPress={handleClick} />

// 자식 컴포넌트 (Button.js)
const Button = ({ title, onPress }) => {
  // title과 onPress를 받아서 사용
};
```

#### 2.2.4 State (상태)

컴포넌트가 기억해야 하는 데이터입니다.

```javascript
const [count, setCount] = useState(0);  // 초기값 0
// count: 현재 값
// setCount: 값을 변경하는 함수
```

---

## 3. 홈 화면 (HomeScreen)

### 3.1 개요

홈 화면은 앱의 메인 대시보드로, 사용자에게 가장 중요한 정보를 한눈에 보여줍니다.

**위치:** `src/screens/HomeScreen.js`

### 3.2 주요 구성 요소

```javascript
const HomeScreen = ({ navigation }) => {
  // 1. 사용자 정보
  const userName = '관리자';
  const userJob = '소프트웨어 개발자';

  // 2. 이슈 지수 데이터
  const globalIssueIndex = { score: 78, trend: 3, ... };
  const jobIssueIndex = { score: 82, trend: 5, ... };

  // 3. 최신 인사이트 목록
  const latestInsights = [ ... ];

  return ( ... );
};
```

### 3.3 코드 단계별 설명

#### 단계 1: 임포트 (Import)

```javascript
import React from 'react';
import { View, Text, StyleSheet, ScrollView } from 'react-native';
```

**설명:**
- `React`: 리액트 라이브러리
- `View, Text`: 화면에 표시할 UI 요소들
- `StyleSheet`: 스타일을 정의하는 도구

#### 단계 2: 데이터 준비

```javascript
const globalIssueIndex = {
  score: 78,           // 현재 지수 점수
  trend: 3,            // 상승/하락 추세
  riskLevel: 'high',   // 위험도
  mainIssues: [        // 주요 이슈 목록
    'Gemini 3 발표',
    'AI 딥페이크 사기 급증',
  ],
};
```

**왜 이렇게 하나요?**
- 실제 앱에서는 서버에서 데이터를 받아오지만,
- 개발 중에는 Mock 데이터(가짜 데이터)로 먼저 작업합니다.

#### 단계 3: UI 구조

```javascript
<SafeAreaView style={styles.container}>
  <ScrollView>
    {/* 인사말 */}
    <Text>안녕하세요, {userName}님!</Text>

    {/* 통합 AI 이슈 지수 */}
    <IssueIndexCard {...globalIssueIndex} />

    {/* 직업별 이슈 지수 */}
    <IssueIndexCard {...jobIssueIndex} />

    {/* 최신 인사이트 */}
    {latestInsights.map(insight => (
      <InsightCard key={insight.id} {...insight} />
    ))}
  </ScrollView>
</SafeAreaView>
```

**핵심 개념:**

1. **SafeAreaView**: 아이폰의 노치(상단 카메라 부분)를 피해서 내용을 표시
2. **ScrollView**: 내용이 많을 때 스크롤 가능하게 만듦
3. **map()**: 배열의 각 항목을 컴포넌트로 변환

#### 단계 4: 차트 표시

```javascript
<LineChart
  data={globalIndexHistory}  // 차트 데이터
  width={Dimensions.get('window').width - 32}  // 화면 너비
  height={220}  // 차트 높이
  chartConfig={{  // 차트 스타일 설정
    color: (opacity = 1) => `rgba(59, 130, 246, ${opacity})`,
  }}
/>
```

**설명:**
- `react-native-chart-kit` 라이브러리 사용
- `Dimensions.get('window').width`: 현재 화면의 너비를 가져옴
- 데이터 형식:
  ```javascript
  {
    labels: ['6개월전', '5개월전', '현재'],
    datasets: [{ data: [45, 52, 78] }]
  }
  ```

#### 단계 5: 스타일링

```javascript
const styles = StyleSheet.create({
  container: {
    flex: 1,  // 전체 화면 차지
    backgroundColor: colors.backgroundGray,
  },
  greeting: {
    paddingVertical: spacing.lg,  // 상하 여백
  },
  greetingText: {
    ...textStyles.h3,  // 미리 정의된 텍스트 스타일 적용
    color: colors.text,
  },
});
```

**초보자 TIP:**
- `flex: 1`: "사용 가능한 공간을 모두 차지해라"
- `padding`: 안쪽 여백
- `margin`: 바깥쪽 여백
- `...textStyles.h3`: spread 연산자로 스타일 복사

### 3.4 주요 기능 동작 원리

#### 네비게이션 (화면 이동)

```javascript
<Button
  title="더 많은 인사이트 보기"
  onPress={() => navigation.navigate('Insights')}
/>
```

**설명:**
- `navigation.navigate('Insights')`: 'Insights' 화면으로 이동
- `onPress`: 버튼을 눌렀을 때 실행될 함수

---

## 4. 인사이트 화면 (InsightFeedScreen)

### 4.1 개요

AI 관련 최신 뉴스와 정보를 카드 형태로 보여주는 피드 화면입니다.

**위치:** `src/screens/InsightFeedScreen.js`

### 4.2 State 관리

```javascript
const InsightFeedScreen = () => {
  const [selectedFilter, setSelectedFilter] = useState('all');

  // 초기값: 'all'
  // selectedFilter: 현재 선택된 필터
  // setSelectedFilter: 필터를 변경하는 함수
};
```

**예시:**
```javascript
// 사용자가 '범죄' 필터를 클릭하면
setSelectedFilter('crime');  // selectedFilter가 'crime'으로 변경됨
```

### 4.3 필터링 로직

```javascript
const filteredInsights =
  selectedFilter === 'all'
    ? insights  // 전체 보기
    : insights.filter((insight) => insight.category === selectedFilter);
    // 선택된 카테고리만 보기
```

**단계별 설명:**

1. 사용자가 '범죄' 버튼 클릭
2. `setSelectedFilter('crime')` 실행
3. `selectedFilter`가 'crime'으로 변경
4. 컴포넌트가 다시 렌더링됨
5. `filteredInsights`가 범죄 관련 인사이트만 포함하게 됨

### 4.4 가로 스크롤 필터 바

```javascript
<ScrollView
  horizontal  // 가로 스크롤
  showsHorizontalScrollIndicator={false}  // 스크롤바 숨김
>
  {filters.map((filter) => (
    <TouchableOpacity
      key={filter.id}
      style={[
        styles.filterButton,
        selectedFilter === filter.id && styles.filterButtonActive
      ]}
      onPress={() => setSelectedFilter(filter.id)}
    >
      <Text>{filter.label}</Text>
    </TouchableOpacity>
  ))}
</ScrollView>
```

**핵심 포인트:**

1. **horizontal**: 세로가 아닌 가로 스크롤
2. **조건부 스타일**:
   ```javascript
   selectedFilter === filter.id && styles.filterButtonActive
   // 선택된 필터면 활성화 스타일 추가
   ```
3. **map()**: 각 필터를 버튼으로 변환

### 4.5 인사이트 카드 표시

```javascript
{filteredInsights.map((insight) => (
  <InsightCard
    key={insight.id}  // React가 각 항목을 구별하기 위한 고유 키
    {...insight}  // insight 객체의 모든 속성을 props로 전달
    onPress={() => {/* 상세 보기 */}}
    onLike={() => {/* 좋아요 */}}
    onComment={() => {/* 댓글 */}}
  />
))}
```

**spread 연산자 (...) 설명:**

```javascript
// 이것은:
<InsightCard {...insight} />

// 이것과 같습니다:
<InsightCard
  id={insight.id}
  title={insight.title}
  description={insight.description}
  category={insight.category}
  // ... 모든 속성
/>
```

### 4.6 빈 상태 처리

```javascript
{filteredInsights.length === 0 && (
  <View style={styles.emptyContainer}>
    <Text>해당 카테고리의 인사이트가 없습니다.</Text>
  </View>
)}
```

**설명:**
- `&&` 연산자: 앞이 참이면 뒤를 실행
- 필터링 결과가 없을 때 사용자에게 메시지 표시

---

## 5. AI 추천 화면 (AIRecommendScreen)

### 5.1 개요

사용자의 작업에 맞는 AI 도구를 추천하는 화면입니다.

**위치:** `src/screens/AIRecommendScreen.js`

### 5.2 여러 State 관리

```javascript
const [taskInput, setTaskInput] = useState('');  // 입력된 작업
const [showResults, setShowResults] = useState(false);  // 결과 표시 여부
```

**왜 State가 여러 개?**
- 각각 독립적으로 변경되는 데이터이기 때문
- `taskInput`: 사용자가 타이핑할 때마다 변경
- `showResults`: 검색 버튼을 눌렀을 때만 변경

### 5.3 입력 처리

```javascript
<TextInput
  style={styles.input}
  placeholder='예: "이미지에서 텍스트 추출"'
  value={taskInput}  // 현재 입력값
  onChangeText={setTaskInput}  // 입력이 변경될 때 State 업데이트
/>
```

**동작 순서:**

1. 사용자가 "이미지"라고 타이핑
2. `onChangeText`가 호출됨
3. `setTaskInput("이미지")`가 실행됨
4. `taskInput`이 "이미지"로 업데이트됨
5. 입력 필드에 "이미지"가 표시됨

### 5.4 조건부 렌더링

```javascript
{!showResults ? (
  <>
    {/* 검색 화면 */}
    <TextInput ... />
    <Button title="추천받기" />
  </>
) : (
  <>
    {/* 결과 화면 */}
    {recommendationResults.map(result => ...)}
  </>
)}
```

**설명:**
- `!showResults`: showResults가 false일 때
- `? ... : ...`: 삼항 연산자 (if-else와 비슷)
- 검색 전/후에 다른 화면을 보여줌

### 5.5 순위 아이콘 함수

```javascript
const getRankIcon = (rank) => {
  switch (rank) {
    case 1: return '🥇';
    case 2: return '🥈';
    case 3: return '🥉';
    default: return '';
  }
};
```

**사용법:**
```javascript
<Text>{getRankIcon(result.rank)}</Text>
// result.rank가 1이면 🥇 표시
```

### 5.6 별점 표시 (동적 생성)

```javascript
<Text>{'⭐'.repeat(Math.floor(result.score / 20))}</Text>
```

**동작 원리:**

```javascript
result.score = 98.5
Math.floor(98.5 / 20) = 4
'⭐'.repeat(4) = '⭐⭐⭐⭐'
```

---

## 6. 이슈지수 화면 (IssueIndexScreen)

### 6.1 개요

AI 이슈 지수의 변화를 추적하고 통계를 제공하는 화면입니다.

**위치:** `src/screens/IssueIndexScreen.js`

### 6.2 복잡한 State 관리

```javascript
const [period, setPeriod] = useState('3months');  // 기간
const [chartData, setChartData] = useState([]);  // 차트 데이터
const [loading, setLoading] = useState(true);  // 로딩 상태
const [refreshing, setRefreshing] = useState(false);  // 새로고침 상태
```

**각 State의 역할:**

1. **period**: 사용자가 선택한 기간 (3개월, 6개월 등)
2. **chartData**: 차트에 표시할 데이터
3. **loading**: 처음 화면 로딩 중인지
4. **refreshing**: 사용자가 당겨서 새로고침 중인지

### 6.3 useEffect Hook

```javascript
useEffect(() => {
  loadData();
}, [period]);  // period가 변경될 때마다 실행
```

**동작 원리:**

1. 컴포넌트가 처음 렌더링될 때 실행
2. `period`가 변경될 때마다 다시 실행
3. 데이터를 다시 불러옴

**예시:**
```javascript
// 사용자가 "6개월" 버튼 클릭
setPeriod('6months');
// period가 변경됨
// useEffect가 감지하고 loadData() 실행
// 6개월 데이터를 불러옴
```

### 6.4 비동기 데이터 로딩

```javascript
const loadData = async () => {
  try {
    setLoading(true);  // 로딩 시작

    const latest = issueIndexService.getLatestUpdate();
    const chart = issueIndexService.getChartData(period);
    const stats = issueIndexService.getStatistics(period);

    setLatestUpdate(latest);
    setChartData(chart);
    setStatistics(stats);
  } catch (error) {
    console.error('데이터 로드 오류:', error);
  } finally {
    setLoading(false);  // 로딩 종료 (성공/실패 상관없이)
  }
};
```

**try-catch-finally 설명:**

- **try**: 정상 실행 코드
- **catch**: 에러 발생 시 실행
- **finally**: 성공/실패 상관없이 항상 실행

### 6.5 로딩 상태 처리

```javascript
if (loading) {
  return (
    <SafeAreaView style={styles.container}>
      <ActivityIndicator size="large" color={colors.primary} />
      <Text>데이터를 불러오는 중...</Text>
    </SafeAreaView>
  );
}
```

**설명:**
- 데이터를 불러오는 동안 로딩 스피너 표시
- 로딩이 끝나면 실제 내용 표시

### 6.6 Pull to Refresh (당겨서 새로고침)

```javascript
<ScrollView
  refreshControl={
    <RefreshControl
      refreshing={refreshing}
      onRefresh={handleRefresh}
    />
  }
>
```

**동작 순서:**

1. 사용자가 화면을 아래로 당김
2. `onRefresh` 호출
3. `handleRefresh()` 실행
4. `setRefreshing(true)` - 새로고침 아이콘 표시
5. 데이터 다시 불러오기
6. `setRefreshing(false)` - 새로고침 완료

### 6.7 조건부 스타일링

```javascript
<Text
  style={[
    styles.comparisonText,
    latestUpdate.comparison_percentage > 0
      ? styles.comparisonPositive  // 양수면 빨강
      : styles.comparisonNegative  // 음수면 초록
  ]}
>
  {latestUpdate.comparison_percentage > 0 ? '↑' : '↓'}
  {latestUpdate.comparison_percentage}%
</Text>
```

**설명:**
- 지수가 올라가면 빨강색 + 위 화살표
- 지수가 내려가면 초록색 + 아래 화살표

---

## 7. 커뮤니티 화면 (CommunityScreen)

### 7.1 개요

사용자들이 정보를 공유하는 게시판 화면입니다.

**위치:** `src/screens/CommunityScreen.js`

### 7.2 실시간 생성 게시판

```javascript
const liveBoards = [
  {
    id: 1,
    title: '🔥 [HOT] Sora 2 발표',
    participants: 234,
    posts: 45,
    isHot: true,  // HOT 뱃지 표시
  },
  // ...
];
```

### 7.3 조건부 뱃지 표시

```javascript
<View style={styles.boardHeader}>
  <Text>{board.title}</Text>

  {board.isHot && (
    <View style={styles.hotBadge}>
      <Text>HOT</Text>
    </View>
  )}

  {board.isNew && (
    <View style={styles.newBadge}>
      <Text>NEW</Text>
    </View>
  )}
</View>
```

**설명:**
- `board.isHot`이 true일 때만 HOT 뱃지 표시
- `&&` 연산자 사용

### 7.4 Floating Button (떠있는 버튼)

```javascript
<TouchableOpacity
  style={styles.floatingButton}
  onPress={handleWritePost}
>
  <Text>✍️</Text>
  <Text>글 작성</Text>
</TouchableOpacity>

// 스타일
floatingButton: {
  position: 'absolute',  // 절대 위치
  bottom: spacing.xl,    // 하단에서 32px
  right: spacing.md,     // 오른쪽에서 16px
  backgroundColor: colors.primary,
  borderRadius: borderRadius.full,  // 완전한 둥근 모서리
  shadowColor: colors.shadowDark,  // 그림자
  shadowOffset: { width: 0, height: 4 },
  shadowOpacity: 0.3,
  elevation: 8,  // Android 그림자
}
```

**핵심 포인트:**
- `position: 'absolute'`: 다른 요소와 독립적으로 위치
- 화면 오른쪽 하단에 고정됨
- 스크롤해도 계속 그 자리에 있음

---

## 8. 공통 컴포넌트

### 8.1 Button 컴포넌트

**위치:** `src/components/common/Button.js`

#### 8.1.1 Props 시스템

```javascript
const Button = ({
  title,              // 버튼 텍스트
  onPress,            // 클릭 시 실행할 함수
  variant = 'primary', // 버튼 스타일 (기본값: primary)
  size = 'medium',    // 버튼 크기 (기본값: medium)
  disabled = false,   // 비활성화 여부
  loading = false,    // 로딩 상태
  fullWidth = false,  // 전체 너비 사용
}) => {
  // ...
};
```

**사용 예시:**

```javascript
// 기본 버튼
<Button title="확인" onPress={handleConfirm} />

// 외곽선 버튼
<Button
  title="취소"
  variant="outline"
  onPress={handleCancel}
/>

// 전체 너비 + 로딩 중
<Button
  title="로그인"
  fullWidth
  loading={isLoading}
  onPress={handleLogin}
/>
```

#### 8.1.2 동적 스타일 생성

```javascript
const getButtonStyle = () => {
  const baseStyle = [
    styles.button,           // 기본 스타일
    styles[variant],         // variant에 따른 스타일
    styles[size]            // size에 따른 스타일
  ];

  if (disabled) baseStyle.push(styles.disabled);
  if (fullWidth) baseStyle.push(styles.fullWidth);

  return baseStyle;
};
```

**설명:**
- 여러 조건에 따라 스타일을 조합
- 배열 형태로 스타일 전달 가능

### 8.2 Card 컴포넌트

**위치:** `src/components/common/Card.js`

#### 8.2.1 조건부 컴포넌트 타입

```javascript
const Card = ({ children, onPress, style, elevation = 2 }) => {
  const CardComponent = onPress ? TouchableOpacity : View;

  return (
    <CardComponent style={[...]} onPress={onPress}>
      {children}
    </CardComponent>
  );
};
```

**핵심 아이디어:**

- `onPress`가 있으면 → TouchableOpacity (클릭 가능)
- `onPress`가 없으면 → View (클릭 불가)
- 같은 컴포넌트를 두 가지 용도로 사용 가능

**사용 예시:**

```javascript
// 클릭 가능한 카드
<Card onPress={() => navigate('Detail')}>
  <Text>클릭하세요</Text>
</Card>

// 정적인 카드
<Card>
  <Text>그냥 카드</Text>
</Card>
```

### 8.3 InsightCard 컴포넌트

**위치:** `src/components/common/InsightCard.js`

#### 8.3.1 카테고리별 아이콘/색상

```javascript
const getCategoryIcon = (cat) => {
  switch (cat) {
    case 'crime': return '🚨';
    case 'regulation': return '⚖️';
    case 'company': return '🚀';
    default: return '📰';
  }
};

const getCategoryColor = (cat) => {
  switch (cat) {
    case 'crime': return colors.danger;      // 빨강
    case 'regulation': return colors.warning; // 오렌지
    case 'company': return colors.info;      // 파랑
    default: return colors.textSecondary;
  }
};
```

**사용:**
```javascript
const categoryColor = getCategoryColor(category);

<View style={[styles.card, { borderLeftColor: categoryColor }]}>
  <Text>{getCategoryIcon(category)}</Text>
</View>
```

#### 8.3.2 동적 위험도 표시

```javascript
{riskLevel && (
  <View style={styles.riskContainer}>
    <Text style={styles.riskLabel}>당신의 위험도:</Text>
    <Text style={[styles.riskValue, { color: categoryColor }]}>
      {riskLevel === 'very_high' ? '🔴🔴🔴 매우 높음' :
       riskLevel === 'high' ? '🔴🔴 높음' :
       riskLevel === 'medium' ? '🟡 중간' :
       '🟢 낮음'}
    </Text>
  </View>
)}
```

**삼항 연산자 체이닝:**
```
조건1 ? 값1 : 조건2 ? 값2 : 조건3 ? 값3 : 기본값
```

---

## 9. 스타일링 시스템

### 9.1 색상 시스템

**위치:** `src/theme/colors.js`

```javascript
export const colors = {
  // Primary Colors
  primary: '#6366F1',      // 메인 인디고 블루
  primaryDark: '#4F46E5',  // 어두운 인디고
  primaryLight: '#818CF8', // 밝은 인디고

  // Text Colors
  text: '#1F2937',           // 기본 텍스트
  textSecondary: '#6B7280',  // 보조 텍스트
  textLight: '#9CA3AF',      // 연한 텍스트

  // Status Colors
  success: '#10B981',  // 성공 (녹색)
  warning: '#F59E0B',  // 경고 (오렌지)
  danger: '#EF4444',   // 위험 (빨강)
};
```

**사용법:**
```javascript
import { colors } from '../theme/colors';

const styles = StyleSheet.create({
  title: {
    color: colors.text,  // 일관된 색상 사용
  },
  button: {
    backgroundColor: colors.primary,
  },
});
```

**장점:**
- 한 곳에서 색상 관리
- 전체 앱의 색상을 쉽게 변경 가능
- 일관성 유지

### 9.2 간격 시스템

**위치:** `src/theme/spacing.js`

```javascript
export const spacing = {
  xs: 4,    // 아주 작은 간격
  sm: 8,    // 작은 간격
  md: 16,   // 중간 간격
  lg: 24,   // 큰 간격
  xl: 32,   // 아주 큰 간격
  xxl: 48,
  xxxl: 64,
};

export const borderRadius = {
  sm: 4,
  md: 8,
  lg: 12,
  xl: 16,
  full: 9999,  // 완전히 둥근 모서리
};
```

**사용법:**
```javascript
import { spacing, borderRadius } from '../theme/spacing';

const styles = StyleSheet.create({
  card: {
    padding: spacing.md,           // 16px
    margin: spacing.lg,            // 24px
    borderRadius: borderRadius.lg, // 12px
  },
  button: {
    paddingVertical: spacing.sm,   // 위아래 8px
    paddingHorizontal: spacing.lg, // 좌우 24px
  },
});
```

**왜 이렇게 하나요?**
- 일관된 간격 유지
- 디자인 시스템 구축
- 수정이 쉬움

### 9.3 타이포그래피 (글꼴)

**위치:** `src/theme/typography.js`

```javascript
export const textStyles = {
  h1: {
    fontSize: 32,
    fontWeight: '700',  // Bold
    lineHeight: 40,
  },
  h2: {
    fontSize: 28,
    fontWeight: '700',
    lineHeight: 36,
  },
  h3: {
    fontSize: 24,
    fontWeight: '600',  // Semi-bold
    lineHeight: 32,
  },
  h4: {
    fontSize: 20,
    fontWeight: '600',
    lineHeight: 28,
  },
  body1: {
    fontSize: 16,
    lineHeight: 24,
  },
  body2: {
    fontSize: 14,
    lineHeight: 20,
  },
  caption: {
    fontSize: 12,
    lineHeight: 16,
  },
};
```

**사용법:**
```javascript
import { textStyles } from '../theme/typography';

const styles = StyleSheet.create({
  title: {
    ...textStyles.h3,  // h3 스타일 전체 복사
    color: colors.text,
  },
  description: {
    ...textStyles.body2,
    color: colors.textSecondary,
  },
});
```

**spread 연산자 (...):**
```javascript
{
  ...textStyles.h3,
  color: colors.text,
}

// 위 코드는 아래와 같음:
{
  fontSize: 24,
  fontWeight: '600',
  lineHeight: 32,
  color: colors.text,
}
```

---

## 10. 초보자를 위한 팁

### 10.1 React Native 기본 개념

#### 10.1.1 JSX 문법

```javascript
// JavaScript와 HTML을 섞어서 사용
const element = (
  <View>
    <Text>안녕하세요</Text>
  </View>
);

// {} 안에 JavaScript 표현식 사용 가능
const name = "철수";
<Text>안녕하세요, {name}님</Text>

// 조건부 렌더링
{isLoggedIn ? <Text>환영합니다</Text> : <Text>로그인하세요</Text>}

// 리스트 렌더링
{items.map(item => <Text key={item.id}>{item.name}</Text>)}
```

#### 10.1.2 Props vs State

**Props (속성):**
- 부모로부터 받는 데이터
- 읽기 전용 (변경 불가)
- 함수의 매개변수와 비슷

```javascript
// 부모
<ChildComponent name="철수" age={25} />

// 자식
const ChildComponent = ({ name, age }) => {
  // name과 age 사용 가능, 변경 불가
};
```

**State (상태):**
- 컴포넌트가 가지는 데이터
- 변경 가능
- 변경되면 화면이 다시 그려짐

```javascript
const [count, setCount] = useState(0);

// 값 읽기
console.log(count);

// 값 변경
setCount(count + 1);  // count가 1 증가
setCount(10);         // count를 10으로 설정
```

#### 10.1.3 이벤트 처리

```javascript
// 기본 형태
<Button onPress={handlePress} />

// 인라인 함수
<Button onPress={() => console.log('클릭!')} />

// 매개변수 전달
<Button onPress={() => handlePress(id)} />

// 함수 정의
const handlePress = (id) => {
  console.log('클릭된 항목:', id);
};
```

### 10.2 자주 쓰는 패턴

#### 10.2.1 조건부 렌더링

**방법 1: && 연산자**
```javascript
{isLoading && <ActivityIndicator />}
// isLoading이 true일 때만 로딩 표시
```

**방법 2: 삼항 연산자**
```javascript
{isLoading ? <ActivityIndicator /> : <Content />}
// isLoading이면 로딩, 아니면 내용 표시
```

**방법 3: 변수 사용**
```javascript
let content;
if (isLoading) {
  content = <ActivityIndicator />;
} else if (hasError) {
  content = <ErrorMessage />;
} else {
  content = <Content />;
}

return <View>{content}</View>;
```

#### 10.2.2 리스트 렌더링

```javascript
const items = [
  { id: 1, name: '사과' },
  { id: 2, name: '바나나' },
  { id: 3, name: '오렌지' },
];

// map() 사용
{items.map(item => (
  <Text key={item.id}>{item.name}</Text>
))}

// key는 왜 필요한가?
// React가 어떤 항목이 변경되었는지 알 수 있게 함
// 고유한 값이어야 함 (보통 id 사용)
```

#### 10.2.3 스타일 조합

```javascript
// 배열로 여러 스타일 적용
<View style={[styles.card, styles.shadow, { marginTop: 10 }]}>

// 조건부 스타일
<View style={[
  styles.button,
  isActive && styles.activeButton,  // isActive일 때만 적용
]}>

// 동적 스타일
<View style={[
  styles.box,
  { backgroundColor: isRed ? 'red' : 'blue' }
]}>
```

### 10.3 디버깅 팁

#### 10.3.1 console.log 활용

```javascript
// 값 확인
console.log('현재 count:', count);

// 객체 확인
console.log('사용자 정보:', user);

// 함수 실행 확인
const handlePress = () => {
  console.log('버튼이 클릭됨!');
  // ...
};
```

#### 10.3.2 주석 활용

```javascript
// 한 줄 주석

/*
  여러 줄
  주석
*/

{/* JSX 안에서의 주석 */}
```

#### 10.3.3 에러 처리

```javascript
try {
  // 실행할 코드
  const data = await fetchData();
  setData(data);
} catch (error) {
  // 에러 발생 시 실행
  console.error('데이터 로드 실패:', error);
  setError(error.message);
}
```

### 10.4 코드 작성 순서

#### 새로운 화면 만들 때:

1. **기본 구조 만들기**
   ```javascript
   const MyScreen = () => {
     return (
       <SafeAreaView>
         <Text>내 화면</Text>
       </SafeAreaView>
     );
   };
   ```

2. **State 추가**
   ```javascript
   const [data, setData] = useState([]);
   ```

3. **데이터 불러오기**
   ```javascript
   useEffect(() => {
     loadData();
   }, []);
   ```

4. **UI 구현**
   ```javascript
   return (
     <SafeAreaView>
       {data.map(item => <ItemCard {...item} />)}
     </SafeAreaView>
   );
   ```

5. **스타일 추가**
   ```javascript
   const styles = StyleSheet.create({
     container: { ... },
   });
   ```

### 10.5 학습 순서 추천

1. **기초 (1-2주)**
   - JavaScript 기본 문법
   - React Native 기본 컴포넌트
   - Props와 State 이해

2. **중급 (2-3주)**
   - Hooks (useState, useEffect)
   - 리스트 렌더링
   - 이벤트 처리

3. **고급 (3-4주)**
   - 네비게이션
   - API 통신
   - 상태 관리

### 10.6 유용한 리소스

- **공식 문서:** https://reactnative.dev/
- **한글 강의:** 노마드 코더, 인프런
- **연습:** Expo Snack (온라인 에디터)

---

## 마무리

### 핵심 요약

1. **컴포넌트**: UI의 작은 조각, 재사용 가능
2. **Props**: 부모에서 자식으로 데이터 전달
3. **State**: 변경 가능한 데이터, 변경 시 화면 업데이트
4. **Hooks**: useState, useEffect 등
5. **스타일링**: StyleSheet, 테마 시스템

### 다음 단계

1. 간단한 컴포넌트부터 직접 만들어보기
2. State를 사용하여 동적 화면 만들기
3. 여러 화면을 연결하는 네비게이션 구현
4. API와 연동하여 실제 데이터 사용

### 자주 하는 실수

1. **key 속성 빼먹기**
   ```javascript
   // ❌ 잘못됨
   {items.map(item => <Text>{item.name}</Text>)}

   // ✅ 올바름
   {items.map(item => <Text key={item.id}>{item.name}</Text>)}
   ```

2. **State 직접 변경**
   ```javascript
   // ❌ 잘못됨
   count = count + 1;

   // ✅ 올바름
   setCount(count + 1);
   ```

3. **비동기 함수에서 await 빼먹기**
   ```javascript
   // ❌ 잘못됨
   const loadData = async () => {
     const data = fetchData();  // await 없음
   };

   // ✅ 올바름
   const loadData = async () => {
     const data = await fetchData();
   };
   ```

---

**작성일:** 2025-11-02
**작성자:** Claude (AI Assistant)
**버전:** 1.0

이 문서가 Ainus 앱 코드를 이해하는 데 도움이 되기를 바랍니다!
궁금한 점이 있으면 언제든 질문하세요. 😊
